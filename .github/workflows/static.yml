# This workflow is disabled. Deployment is handled by deploy.yml
name: Deploy static content to Pages (disabled)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use deploy.yml instead"
